书P201~P202

1. **停止CPU访问主存**：
	- DMA发送停止信号，CPU停止访问主存
	- DMA传送数据
	- DMA通知主存可以使用主存
2. **周期挪用（或周期窃取）**：
	- DMA发送请求，I/O设备挪用总线一个或几个主存周期
	- DMA不请求时CPU正常访问主存
3. **DMA和CPU交替访问**：
	- 没有对总线使用权的申请等过程
	- 各自拥有独立的访存地址寄存器、数据寄存器和读/写信号
	- 二者访存周期相互独立