书P85~P87

*先将原信息读出，再由刷新放大器形成原信息并重新写入的再生过程*

DRAM利用存储元中的电容存储信息，长时间不进行读/写操作，电荷会慢慢流失，因此需要定期为电容补充电荷，称为刷新

1. **集中刷新**：在规定的一个刷新周期内，对全部存储单元集中一段时间逐行进行刷新，此刻必须停止读/写操作，称为“死时间”
2. **分散刷新**：对每行存储单元的刷新分散到每个存取周期内完成
3. **异步刷新**：前两种方法的结合，既可缩短“死时间”，又充分利用最大刷新间隔