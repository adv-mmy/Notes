书P104~P107

- 采用多体模块组成的存储器
- 每个模块有相同的容量和存取速度，各模块都有独立的MAR、MDR、地址译码、驱动电路和读/写电路
- 能并行工作，又能交叉工作

-  编址方式：
	1. 高位交叉：
		- 高位地址表示体号，低位地址表示体内地址
		- 程序按体内地址顺序存放，一个体存满后，再存入下一个体
	2. 低位交叉：
		- 低位地址表示体号，高位地址表示体内地址
		- 程序连续存放在相邻体中（*交叉存储*）