书P348~P353
- **结构相关**：
	- 不同指令争用同一功能部件
	- 解决：
		- 前一条指令对数据存储器访存时暂停后一条指令对指令存储器访存
		- 分开存放数据和指令（*哈佛结构*）
- **数据相关**：
	- 后续程序的操作数需要由前驱程序提供
	- 解决：
		- 停顿后续程序的运行
		- 将前驱程序的结果由旁路直接送到后续指令需要的地方，而不必等写入存储器后再从存储器取数
- **控制相关**：
	- 主要由转移指令引起，后续执行哪个程序等待前驱程序的操作结果，导致期间指令报废
	- 解决：
		- 判别转移是否发生
		- 尽早生成转移目标地址